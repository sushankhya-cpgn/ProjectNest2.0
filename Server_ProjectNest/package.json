{
  "name": "projectnest-server",
  "version": "1.0.0",
  "author": "",
  "license": "ISC",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --ext js,json --watch ./ --env-file config.env server.js",
    "startt": "NODE_ENV=production nodemon --env-file config.env server.js",
    "start": "SET NODE_ENV=production nodemon --env-file config.env server.js"
  },
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "bcryptjs": "^2.4.3",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.3",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.7.5",
    "validator": "^13.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
